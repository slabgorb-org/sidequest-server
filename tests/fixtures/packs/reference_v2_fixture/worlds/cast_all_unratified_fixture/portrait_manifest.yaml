# Public NPC projection for the Story 75-13 all-unratified edge fixture (ADR-138 §D4).
#
# Exactly ONE character, and it is an unratified phantom (observation_pending:
# true). The ratification gate withholds it, so the rendered page has NO Cast
# section at all — but the reference.npc_unratified_skipped span must still fire
# with count == 1. This pins that the skip count is derived from the raw authored
# entries (the gate input) rather than the post-filter survivor set (which is
# empty here).
#
# Slug: "Threnody Vane" -> threnody_vane (withheld — never rendered).

characters:
  - name: Threnody Vane
    role: a name spoken once at the docks
    appearance: >-
      A rumor of a person — the narrator's one-off mention, never re-cited.
    observation_pending: true
