version: "0.1.0"
world: test_world

# Intentionally empty authored roster. This WWN fixture world exercises the
# class roster, equipment tables, and chargen — NOT the authored-NPC path; no
# test reads its NPCs (authored-placement tests use SimpleNamespace stand-ins).
#
# It previously authored a barsoom roster under the WRONG top-level key
# (`authored_npcs:` instead of `npcs:`), so the loader silently loaded it as []
# — the exact regression fixed in story 158-32. The loader now fails loud on a
# present-but-keyless npcs.yaml, so this file declares its empty roster
# explicitly under the correct `npcs:` key.
npcs: []
