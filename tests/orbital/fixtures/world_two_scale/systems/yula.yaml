# Two-scale fixture (Story 98-2) — YULA per-system orrery file.
# Post-98-1 shape: one parent-less primary (yula) + its children, NO top-level
# orbits.yaml monolith. Mirrors the real perseus_cloud/systems/yula.yaml split.
# Synthetic TEST content — not shipped.
version: "0.1.0"
clock:
  epoch_days: 0
travel:
  realism: orbital
bodies:
  yula:
    type: star
    label: YULA
  yula_anchorage:
    type: habitat
    parent: yula
    semi_major_au: 0.5
    period_days: 40
    epoch_phase_deg: 0
    label: "YULA ANCHORAGE"
