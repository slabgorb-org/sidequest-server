# Retirement-stub monolith (Story 98-2, AC1/AC3 edge case fixture).
# A stray top-level orbits.yaml left behind by an incomplete split. Its sole
# body 'legacy_monolith_marker' exists ONLY here — so any test can prove the
# per-region loader did NOT read this file by asserting the marker is absent
# from the resolved content. Per No Silent Fallbacks, a missing systems/<id>.yaml
# must fail loud rather than silently loading this stub.
# Synthetic TEST content — not shipped.
version: "0.1.0"
clock:
  epoch_days: 0
travel:
  realism: orbital
bodies:
  legacy_monolith_marker:
    type: star
    label: "LEGACY MONOLITH"
