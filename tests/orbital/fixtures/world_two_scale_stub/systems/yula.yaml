# Two-scale + retirement-stub fixture (Story 98-2, AC1/AC3 edge case).
# This world has BOTH a per-system file (systems/yula.yaml) AND a stray
# top-level orbits.yaml retirement stub. Per AC1 ("no longer reading
# world_dir/orbits.yaml") and AC3 edge case ("the retirement-stub form of
# orbits.yaml must NOT satisfy resolution"), per-region resolution must read
# THIS file for region 'yula' and must NEVER fall back to the stub.
# Synthetic TEST content — not shipped.
version: "0.1.0"
clock:
  epoch_days: 0
travel:
  realism: orbital
bodies:
  yula:
    type: star
    label: YULA
  yula_anchorage:
    type: habitat
    parent: yula
    semi_major_au: 0.5
    period_days: 40
    epoch_phase_deg: 0
    label: "YULA ANCHORAGE"
